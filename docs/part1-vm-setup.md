# Part 1: Setting Up the Virtual Environment

1. Install VMware Workstation on the host Windows computer.
2. Download and install **Windows Server 2025** as a VM. This will become our domain controller.
3. Download and install **Windows 11 Pro** twice as two separate VMs. These will act as our client PCs.

**Result:** Three VMs running — one server and two clients.
